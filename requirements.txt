# SwingAdvisorBot — Module 1: Data Layer
# Python 3.11+ required
#
# Install: pip install -r requirements.txt
#
# Package purposes:
#   kiteconnect     — Zerodha Kite Connect SDK for NSE stock data, VIX, sector indices
#   httpx           — Async HTTP client for NewsAPI and FRED API calls
#   pydantic        — Data models with validation (v2 required for model_validator)
#   python-dotenv   — Load API keys from .env file
#   crewai          — Agent framework base class (full use in Module 2+)
#   fastapi         — MCP server exposing data tools as HTTP endpoints
#   uvicorn         — ASGI server to run FastAPI

kiteconnect>=5.0.0
httpx>=0.27.0
pydantic>=2.0.0
python-dotenv>=1.0.0
crewai>=0.80.0
fastapi>=0.115.0
uvicorn>=0.32.0
apscheduler>=3.10.0
